# HaiP 개발 작업 규칙

## 이슈

하나의 결과물을 하나의 Issue로 관리한다. 유형은 아래 셋만 쓴다.

| 유형 | 언제 쓰나 | 라벨 | 브랜치 접두사 |
|---|---|---|---|
| 기능 개발 | 없던 것을 새로 만들 때 | `feat` | `feat/` |
| 수정·개선 | 잘못 동작하는 것을 고치거나, 있는 것을 더 낫게 만들 때 | `fix` | `fix/` |
| 리팩터링 | 동작은 그대로 두고 코드만 정리할 때 | `refactor` | `refactor/` |

라벨 이름과 브랜치 접두사는 같은 단어를 쓴다.

- Issue에는 목적, 세부 작업, 완료 조건을 쓴다. (이슈 템플릿 사용)
- 담당자는 Assignees에 지정하고, 기한은 본문에 기록한다.
- 같은 업무의 추가 수정은 기존 Issue에서 이어간다.
- 세부 작업은 체크리스트로 관리하고 실제로 끝난 항목만 체크한다.
- 상태는 미결(Open)과 완료(Closed)만 사용한다.

## 브랜치

`<접두사>/<이슈번호>-<짧은 설명>`

```
feat/42-landing-hero
fix/51-form-validation
refactor/60-api-client
```

- 설명은 영문 소문자와 하이픈으로 쓴다. 애매하면 `feat/42`처럼 번호만 써도 된다.
- main 직접 푸시 금지.

## 커밋

한 줄 요약 끝에 Issue 번호를 붙인다.

```
기능 소개 페이지 추가 (#42)
```

- 커밋 수와 체크리스트 항목 수를 맞출 필요는 없다.
- 형식은 이 이상 강제하지 않는다. 무슨 일을 했는지 읽히면 된다.

## PR

- 제목은 Issue 제목을 그대로 써도 된다.
- 본문에 `Closes #N`을 반드시 넣는다. 머지되면 Issue가 자동으로 닫힌다.
- 배포가 필요한 업무는 로컬에서 동작을 확인한 뒤 머지하고, 머지된 `main`에서 배포한다. 배포 뒤 실제 동작 확인 결과는 Issue 댓글에 남긴다.
- 작업 브랜치에서 운영 배포하지 않는다. `main`이 곧 운영이다.
- 배포는 사람이 직접 실행한다(자동 배포 없음). 레포에 배포 스크립트가 있으면 그것으로 한다.
- 리뷰어는 필요할 때만 지정한다. 혼자 한 작업은 셀프 머지해도 된다.
- PR 없이 진행한 업무는 담당자가 결과를 확인한 뒤 Issue를 직접 닫는다.

## 작업 기록

- 작업 후 Issue 댓글에 변경 내용, 확인 방법, 배포 여부를 짧게 남긴다.

## AI 작업

- 작업 전에 관련 Issue를 찾고, 없으면 생성한다.
- 담당자와 기한은 임의로 정하지 않는다.
- 작업 결과에 맞춰 Issue 체크리스트를 갱신한다.
- 운영 배포와 자동 배포를 유발하는 Push는 사용자가 허용한 범위에서 수행한다.
- GitHub 기록에 실패하면 누락된 내용을 사용자에게 알린다.
